\ProvidesPackage{test}

\usepackage{amsopn} % LaTeXParser-Skip

\usepackage{amsmath}

\usepackage[opt=true,opt2]{test}

\usepackage[
    opt=true,
    opt2
]{test2}

% ignored lines
% LaTeXParser-Off
\newcommand{\ignoredblock}{ignored}
% LaTeXParser-On
\newcommand{\ignoredline}{ignored} % LaTeXParser-Skip

% \newcommand without argument
\newcommand{\nn}{\nonumber}
\newcommand{\eq}{=}
\newcommand{\eqq}{\equiv}

% \newcommand with argument
\newcommand{\set}[1]{\{ #1 \}}
\newcommand{\Fpq}[5]{\,{}_{#1}F_{#2}\biggl(\genfrac..{0pt}{}{#3}{#4};#5\biggr)}

% \providecommand is rare and will be ignored
% \renewcommand should be treated as \newcommand
\providecommand{\bra}[1]{{\langle {#1} |}}
\renewcommand{\bra}[1]{{\langle {#1} |}}

% \newcommand with line break
\newcommand{\textInMath}[1]{%
    \,
    \text{ #1 }
    \,
}

% \newcommand with nested {}
\newcommand{\mathred}[1]{{\color{red} #1}}
\newcommand{\tp}{%
    {\scriptscriptstyle \mathsf{T} }
}

% \newcommand in another {}
\AtBeginDocument{\renewcommand{\check}[1]{\widecheck{#1}}}

% LaTeXParser-Off
\newcommand{\ignoredblocktwo}{ignored}
% LaTeXParser-On

\newcommand{\ignoredMathJax}{ignored} % LaTeXParser-MathJax-Skip

\newcommand{\ignoredLaTeXWorkshop}{ignored} % LaTeXParser-LaTeXWorkshop-Skip